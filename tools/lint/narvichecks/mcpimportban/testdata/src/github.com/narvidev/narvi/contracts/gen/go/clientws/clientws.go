// Package clientws stands for an UNRELATED contracts/gen/go/* sibling of
// restdtos -- the client WebSocket protocol's own wire DTOs, entirely
// unrelated to this surface. contracts/gen/go/restdtos is allowed by
// EXACT match only, never as a prefix of contracts/gen/go/* at large, so
// this sibling must still be banned.
package clientws

type Envelope struct{}
