// Code generated by github.com/atombender/go-jsonschema, DO NOT EDIT.

package sandboxws

import "encoding/json"
import "fmt"
import "reflect"
import "regexp"
import "time"

// Acknowledge one of the 6 critical event types by its deterministic ackId (§6.1
// ack protocol).
type Ack struct {
	// The ackId being acknowledged, formatted '{type}:{messageId}' (§6.1).
	AckId string `json:"ackId" yaml:"ackId" mapstructure:"ackId"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *Ack) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["ackId"]; raw != nil && !ok {
		return fmt.Errorf("field ackId in Ack: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in Ack: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in Ack: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in Ack: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in Ack: required")
	}
	type Plain Ack
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "ack" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "ack")
	}
	*j = Ack(plain)
	return nil
}

// artifactType MUST match the Postgres artifact_type enum
// (migrations/000012_artifacts.up.sql) exactly. status/failureReason (§28.6) are
// additive and OPTIONAL: absent status means "ready" (the same
// zero-producers-today additive reasoning SnapshotReady.commandMessageId used) --
// every pr/preview artifact event emitted before this Step, and every future one
// that never sets them, stays a valid, unchanged shape. Both fields are
// CP-SYNTHESIZED ONLY (mirrors GitSync's own "repo" field note above and the
// general subTaskId-population convention this file's own top-level description
// states): the sandbox never emits an artifact event for an upload at all -- the
// control plane already owns the row before any bytes exist, so a sandbox-reported
// completion would be a second writer over a fact Postgres already owns (§5.1).
// failureReason MUST match the Postgres artifact_failure_reason enum
// (migrations/000060_artifacts_upload_lifecycle.up.sql) exactly, and is only ever
// non-null when status is "failed".
type Artifact struct {
	// ArtifactType corresponds to the JSON schema field "artifactType".
	ArtifactType ArtifactArtifactType `json:"artifactType" yaml:"artifactType" mapstructure:"artifactType"`

	// Matches Postgres artifact_failure_reason exactly. Null/absent except when
	// status is "failed".
	FailureReason *ArtifactFailureReason `json:"failureReason,omitempty,omitzero" yaml:"failureReason,omitempty" mapstructure:"failureReason,omitempty"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// Metadata corresponds to the JSON schema field "metadata".
	Metadata ArtifactMetadata `json:"metadata" yaml:"metadata" mapstructure:"metadata"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Absent means "ready" -- see this definition's own top-level description for the
	// additive/CP-synthesized-only contract.
	Status *ArtifactStatus `json:"status,omitempty,omitzero" yaml:"status,omitempty" mapstructure:"status,omitempty"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`

	// format is deliberately "uri-reference", not the stricter "uri" (§8.6 relaxation
	// -- backward compatible: every absolute URL a plain "uri" ever accepted still
	// validates, so no pr/preview producer's existing behavior changes). pr/preview
	// artifacts always carry an ABSOLUTE external link (a GitHub PR/preview URL);
	// upload artifacts carry the artifacts row's own STABLE, RELATIVE
	// /api/sessions/{id}/uploads/{uploadId}/content path (§28.5: "the artifact row's
	// url column stores this stable /api/... content path, never a presigned URL") --
	// a relative reference a browser client resolves against its own current origin,
	// which "uri" alone would have rejected.
	Url string `json:"url" yaml:"url" mapstructure:"url"`
}

type ArtifactArtifactType string

const ArtifactArtifactTypePr ArtifactArtifactType = "pr"
const ArtifactArtifactTypePreview ArtifactArtifactType = "preview"
const ArtifactArtifactTypeUpload ArtifactArtifactType = "upload"

var enumValues_ArtifactArtifactType = []interface{}{
	"pr",
	"preview",
	"upload",
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *ArtifactArtifactType) UnmarshalJSON(value []byte) error {
	var v string
	if err := json.Unmarshal(value, &v); err != nil {
		return err
	}
	var ok bool
	for _, expected := range enumValues_ArtifactArtifactType {
		if reflect.DeepEqual(v, expected) {
			ok = true
			break
		}
	}
	if !ok {
		return fmt.Errorf("invalid value (expected one of %#v): %#v", enumValues_ArtifactArtifactType, v)
	}
	*j = ArtifactArtifactType(v)
	return nil
}

type ArtifactFailureReason struct {
	Value interface{}
}

// MarshalJSON implements json.Marshaler.
func (j *ArtifactFailureReason) MarshalJSON() ([]byte, error) {
	return json.Marshal(j.Value)
}

var enumValues_ArtifactFailureReason = []interface{}{
	"size_exceeded",
	"quota_exceeded",
	"verification_failed",
	"abandoned",
	nil,
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *ArtifactFailureReason) UnmarshalJSON(value []byte) error {
	var v struct {
		Value interface{}
	}
	if err := json.Unmarshal(value, &v.Value); err != nil {
		return err
	}
	var ok bool
	for _, expected := range enumValues_ArtifactFailureReason {
		if reflect.DeepEqual(v.Value, expected) {
			ok = true
			break
		}
	}
	if !ok {
		return fmt.Errorf("invalid value (expected one of %#v): %#v", enumValues_ArtifactFailureReason, v.Value)
	}
	*j = ArtifactFailureReason(v)
	return nil
}

type ArtifactMetadata map[string]interface{}

type ArtifactStatus string

const ArtifactStatusFailed ArtifactStatus = "failed"
const ArtifactStatusReady ArtifactStatus = "ready"

var enumValues_ArtifactStatus = []interface{}{
	"ready",
	"failed",
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *ArtifactStatus) UnmarshalJSON(value []byte) error {
	var v string
	if err := json.Unmarshal(value, &v); err != nil {
		return err
	}
	var ok bool
	for _, expected := range enumValues_ArtifactStatus {
		if reflect.DeepEqual(v, expected) {
			ok = true
			break
		}
	}
	if !ok {
		return fmt.Errorf("invalid value (expected one of %#v): %#v", enumValues_ArtifactStatus, v)
	}
	*j = ArtifactStatus(v)
	return nil
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *Artifact) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["artifactType"]; raw != nil && !ok {
		return fmt.Errorf("field artifactType in Artifact: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in Artifact: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in Artifact: required")
	}
	if _, ok := raw["metadata"]; raw != nil && !ok {
		return fmt.Errorf("field metadata in Artifact: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in Artifact: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in Artifact: required")
	}
	if _, ok := raw["url"]; raw != nil && !ok {
		return fmt.Errorf("field url in Artifact: required")
	}
	type Plain Artifact
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "artifact" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "artifact")
	}
	*j = Artifact(plain)
	return nil
}

// Named boot phase report; re-arms the connecting deadline during long boots
// (§3.2).
type BootProgress struct {
	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// Phase corresponds to the JSON schema field "phase".
	Phase string `json:"phase" yaml:"phase" mapstructure:"phase"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Timestamp corresponds to the JSON schema field "timestamp".
	Timestamp time.Time `json:"timestamp" yaml:"timestamp" mapstructure:"timestamp"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *BootProgress) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in BootProgress: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in BootProgress: required")
	}
	if _, ok := raw["phase"]; raw != nil && !ok {
		return fmt.Errorf("field phase in BootProgress: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in BootProgress: required")
	}
	if _, ok := raw["timestamp"]; raw != nil && !ok {
		return fmt.Errorf("field timestamp in BootProgress: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in BootProgress: required")
	}
	type Plain BootProgress
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "boot_progress" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "boot_progress")
	}
	*j = BootProgress(plain)
	return nil
}

// §33.3: best-effort relay of ONE sandbox_agent_*_duration_seconds data point
// sandbox-agent has ALREADY measured (its own time.Since bracket) -- the control
// plane records it into the matching histogram
// (internal/app/sessionactor/opsmetrics.go) instead of sandbox-agent recording it
// locally and risking losing it to the ephemeral sandbox's own
// bounded-shutdown-flush/SIGKILL exposure (§33.1). NOT critical (no ackId):
// telemetry never earns a place among §6.1's critical acked types, and its loss
// must never fail a boot or a turn (§33.3 point 1). 'metric' selects which of the
// four sandbox-emitted histograms (§33.1) this data point belongs to; 'seconds' is
// the exact already-measured value, never re-derived control-plane-side (§33.2
// records why control-plane derivation was tried and rejected for all four). Every
// property below 'seconds' is that instrument's own low-cardinality tag set --
// only the subset naming this event's own 'metric' applies, per each property's
// own description -- EXCEPT 'repo', which rides this event for per-session
// debugging (the events log) only and is deliberately NEVER treated as a metric
// attribute: an unbounded-cardinality value (§33.3 point 3), the identical
// repo-is-log-only split GitSync's own 'repo' property note above already
// documents for a sibling event.
type BootTiming struct {
	// boot_duration and hook_rerun_duration only: the sandboxboot.BootMode this data
	// point ran under. Null/absent for git_fetch_duration/git_checkout_duration.
	BootMode BootTimingBootMode `json:"bootMode,omitempty,omitzero" yaml:"bootMode,omitempty" mapstructure:"bootMode,omitempty"`

	// git_fetch_duration only (§19.3's own degrade policy). Null/absent for every
	// other metric.
	Degraded BootTimingDegraded `json:"degraded,omitempty,omitzero" yaml:"degraded,omitempty" mapstructure:"degraded,omitempty"`

	// boot_duration/hook_rerun_duration/git_checkout_duration only: whether the
	// measured operation itself failed. Null/absent for git_fetch_duration, which
	// uses 'degraded' instead -- a boot-time fetch failure the §19.3 degrade policy
	// allowed to proceed is a distinct fact from a hard failure.
	Failed BootTimingFailed `json:"failed,omitempty,omitzero" yaml:"failed,omitempty" mapstructure:"failed,omitempty"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// hook_rerun_duration only: the hook script name (sandboxboot.Hook --
	// setup.sh/start.sh). Null/absent for every other metric.
	Hook BootTimingHook `json:"hook,omitempty,omitzero" yaml:"hook,omitempty" mapstructure:"hook,omitempty"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// Which of §33.1's four sandbox-emitted histograms
	// (sandbox_agent_boot_duration_seconds,
	// sandbox_agent_hook_rerun_duration_seconds,
	// sandbox_agent_git_fetch_duration_seconds,
	// sandbox_agent_git_checkout_duration_seconds) this data point belongs to.
	Metric BootTimingMetric `json:"metric" yaml:"metric" mapstructure:"metric"`

	// hook_rerun_duration/git_fetch_duration/git_checkout_duration only: which repo
	// (SessionConfig.repos[].name) this data point is about. Null/absent for
	// boot_duration, a whole-boot measurement with no single repo. See this
	// definition's own top-level description for why this rides the event log only
	// and is never a metric attribute.
	Repo BootTimingRepo `json:"repo,omitempty,omitzero" yaml:"repo,omitempty" mapstructure:"repo,omitempty"`

	// The already-measured wall-clock duration, in seconds -- the SAME time.Since
	// bracket the deleted sandbox-side histogram used to record locally. Never
	// recomputed control-plane-side.
	Seconds float64 `json:"seconds" yaml:"seconds" mapstructure:"seconds"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`

	// hook_rerun_duration only (§19.4). Null/absent for every other metric.
	WorkspaceMoved BootTimingWorkspaceMoved `json:"workspaceMoved,omitempty,omitzero" yaml:"workspaceMoved,omitempty" mapstructure:"workspaceMoved,omitempty"`
}

// boot_duration and hook_rerun_duration only: the sandboxboot.BootMode this data
// point ran under. Null/absent for git_fetch_duration/git_checkout_duration.
type BootTimingBootMode *string

// git_fetch_duration only (§19.3's own degrade policy). Null/absent for every
// other metric.
type BootTimingDegraded *bool

// boot_duration/hook_rerun_duration/git_checkout_duration only: whether the
// measured operation itself failed. Null/absent for git_fetch_duration, which uses
// 'degraded' instead -- a boot-time fetch failure the §19.3 degrade policy allowed
// to proceed is a distinct fact from a hard failure.
type BootTimingFailed *bool

// hook_rerun_duration only: the hook script name (sandboxboot.Hook --
// setup.sh/start.sh). Null/absent for every other metric.
type BootTimingHook *string

type BootTimingMetric string

const BootTimingMetricBootDuration BootTimingMetric = "boot_duration"
const BootTimingMetricGitCheckoutDuration BootTimingMetric = "git_checkout_duration"
const BootTimingMetricGitFetchDuration BootTimingMetric = "git_fetch_duration"
const BootTimingMetricHookRerunDuration BootTimingMetric = "hook_rerun_duration"

var enumValues_BootTimingMetric = []interface{}{
	"boot_duration",
	"hook_rerun_duration",
	"git_fetch_duration",
	"git_checkout_duration",
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *BootTimingMetric) UnmarshalJSON(value []byte) error {
	var v string
	if err := json.Unmarshal(value, &v); err != nil {
		return err
	}
	var ok bool
	for _, expected := range enumValues_BootTimingMetric {
		if reflect.DeepEqual(v, expected) {
			ok = true
			break
		}
	}
	if !ok {
		return fmt.Errorf("invalid value (expected one of %#v): %#v", enumValues_BootTimingMetric, v)
	}
	*j = BootTimingMetric(v)
	return nil
}

// hook_rerun_duration/git_fetch_duration/git_checkout_duration only: which repo
// (SessionConfig.repos[].name) this data point is about. Null/absent for
// boot_duration, a whole-boot measurement with no single repo. See this
// definition's own top-level description for why this rides the event log only and
// is never a metric attribute.
type BootTimingRepo *string

// hook_rerun_duration only (§19.4). Null/absent for every other metric.
type BootTimingWorkspaceMoved *bool

// UnmarshalJSON implements json.Unmarshaler.
func (j *BootTiming) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in BootTiming: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in BootTiming: required")
	}
	if _, ok := raw["metric"]; raw != nil && !ok {
		return fmt.Errorf("field metric in BootTiming: required")
	}
	if _, ok := raw["seconds"]; raw != nil && !ok {
		return fmt.Errorf("field seconds in BootTiming: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in BootTiming: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in BootTiming: required")
	}
	type Plain BootTiming
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "boot_timing" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "boot_timing")
	}
	*j = BootTiming(plain)
	return nil
}

// CRITICAL (requires ackId). outcome MUST match the turn_status terminal states
// (migrations/000005_turns.up.sql).
type ExecutionComplete struct {
	// Deterministic ackId = 'execution_complete:{messageId}' (§6.1).
	AckId string `json:"ackId" yaml:"ackId" mapstructure:"ackId"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// Outcome corresponds to the JSON schema field "outcome".
	Outcome ExecutionCompleteOutcome `json:"outcome" yaml:"outcome" mapstructure:"outcome"`

	// Reason corresponds to the JSON schema field "reason".
	Reason ExecutionCompleteReason `json:"reason" yaml:"reason" mapstructure:"reason"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// §6.1/§7.1 sub-task fan-out: OPTIONAL — absent or null means this event belongs
	// to the turn's main lane; a non-null value is the subTaskId (same id
	// sub_task_start/sub_task_finish carry) of the sub-task lane this event belongs
	// to.
	SubTaskId ExecutionCompleteSubTaskId `json:"subTaskId,omitempty,omitzero" yaml:"subTaskId,omitempty" mapstructure:"subTaskId,omitempty"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

type ExecutionCompleteOutcome string

const ExecutionCompleteOutcomeCancelled ExecutionCompleteOutcome = "cancelled"
const ExecutionCompleteOutcomeCompleted ExecutionCompleteOutcome = "completed"
const ExecutionCompleteOutcomeFailed ExecutionCompleteOutcome = "failed"

var enumValues_ExecutionCompleteOutcome = []interface{}{
	"completed",
	"failed",
	"cancelled",
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *ExecutionCompleteOutcome) UnmarshalJSON(value []byte) error {
	var v string
	if err := json.Unmarshal(value, &v); err != nil {
		return err
	}
	var ok bool
	for _, expected := range enumValues_ExecutionCompleteOutcome {
		if reflect.DeepEqual(v, expected) {
			ok = true
			break
		}
	}
	if !ok {
		return fmt.Errorf("invalid value (expected one of %#v): %#v", enumValues_ExecutionCompleteOutcome, v)
	}
	*j = ExecutionCompleteOutcome(v)
	return nil
}

type ExecutionCompleteReason *string

// §6.1/§7.1 sub-task fan-out: OPTIONAL — absent or null means this event belongs
// to the turn's main lane; a non-null value is the subTaskId (same id
// sub_task_start/sub_task_finish carry) of the sub-task lane this event belongs
// to.
type ExecutionCompleteSubTaskId *string

// UnmarshalJSON implements json.Unmarshaler.
func (j *ExecutionComplete) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["ackId"]; raw != nil && !ok {
		return fmt.Errorf("field ackId in ExecutionComplete: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in ExecutionComplete: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in ExecutionComplete: required")
	}
	if _, ok := raw["outcome"]; raw != nil && !ok {
		return fmt.Errorf("field outcome in ExecutionComplete: required")
	}
	if _, ok := raw["reason"]; raw != nil && !ok {
		return fmt.Errorf("field reason in ExecutionComplete: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in ExecutionComplete: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in ExecutionComplete: required")
	}
	type Plain ExecutionComplete
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if matched, _ := regexp.MatchString(`^execution_complete:.+$`, string(plain.AckId)); !matched {
		return fmt.Errorf("field %s pattern match: must match %s", "AckId", `^execution_complete:.+$`)
	}
	if plain.Type != "execution_complete" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "execution_complete")
	}
	*j = ExecutionComplete(plain)
	return nil
}

// §3.4 stash -> checkout -> pop sequence, reported as it happens. NOTE on "repo"
// in required: this tightens an already-merged def (repo did not exist at all in
// the earlier scaffolding-only shape) rather than staying purely additive. That is
// deliberate and safe here, not an oversight: git_sync had zero real producers or
// consumers before the same Step that added "repo" also wired the one producer
// that sets it (cmd/sandbox-agent) and the field is read nowhere else, so no
// already-running wire party could have been depending on its absence. Tightening
// a field that is actually in use elsewhere would need a BREAKING marker instead.
type GitSync struct {
	// Branch corresponds to the JSON schema field "branch".
	Branch string `json:"branch" yaml:"branch" mapstructure:"branch"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// §3.4: repos are always a list -- names which repo (SessionConfig.repos[].name)
	// this phase is about, so a multi-repo session reconciling several repos
	// concurrently/sequentially can be disambiguated.
	Repo string `json:"repo" yaml:"repo" mapstructure:"repo"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Status corresponds to the JSON schema field "status".
	Status GitSyncStatus `json:"status" yaml:"status" mapstructure:"status"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// Acknowledge completion of the CP-side half of a git_sync step
// (stash/checkout/pop, §3.4). No fields beyond the common envelope.
type GitSyncComplete struct {
	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *GitSyncComplete) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in GitSyncComplete: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in GitSyncComplete: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in GitSyncComplete: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in GitSyncComplete: required")
	}
	type Plain GitSyncComplete
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "git_sync_complete" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "git_sync_complete")
	}
	*j = GitSyncComplete(plain)
	return nil
}

type GitSyncStatus string

const GitSyncStatusCheckout GitSyncStatus = "checkout"
const GitSyncStatusPop GitSyncStatus = "pop"
const GitSyncStatusStash GitSyncStatus = "stash"

var enumValues_GitSyncStatus = []interface{}{
	"stash",
	"checkout",
	"pop",
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *GitSyncStatus) UnmarshalJSON(value []byte) error {
	var v string
	if err := json.Unmarshal(value, &v); err != nil {
		return err
	}
	var ok bool
	for _, expected := range enumValues_GitSyncStatus {
		if reflect.DeepEqual(v, expected) {
			ok = true
			break
		}
	}
	if !ok {
		return fmt.Errorf("invalid value (expected one of %#v): %#v", enumValues_GitSyncStatus, v)
	}
	*j = GitSyncStatus(v)
	return nil
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *GitSync) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["branch"]; raw != nil && !ok {
		return fmt.Errorf("field branch in GitSync: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in GitSync: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in GitSync: required")
	}
	if _, ok := raw["repo"]; raw != nil && !ok {
		return fmt.Errorf("field repo in GitSync: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in GitSync: required")
	}
	if _, ok := raw["status"]; raw != nil && !ok {
		return fmt.Errorf("field status in GitSync: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in GitSync: required")
	}
	type Plain GitSync
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "git_sync" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "git_sync")
	}
	*j = GitSync(plain)
	return nil
}

// §6.1: every 30s; carries conversation id + last_boot_phase so liveness
// (last_seen_at, §3.2) and turn-resume state stay current even with no other
// traffic.
type Heartbeat struct {
	// Null before the first turn has started a conversation.
	ConversationId HeartbeatConversationId `json:"conversationId" yaml:"conversationId" mapstructure:"conversationId"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// Null once boot has completed (no more boot phases to report).
	LastBootPhase HeartbeatLastBootPhase `json:"lastBootPhase" yaml:"lastBootPhase" mapstructure:"lastBootPhase"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Timestamp corresponds to the JSON schema field "timestamp".
	Timestamp time.Time `json:"timestamp" yaml:"timestamp" mapstructure:"timestamp"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// Null before the first turn has started a conversation.
type HeartbeatConversationId *string

// Null once boot has completed (no more boot phases to report).
type HeartbeatLastBootPhase *string

// UnmarshalJSON implements json.Unmarshaler.
func (j *Heartbeat) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["conversationId"]; raw != nil && !ok {
		return fmt.Errorf("field conversationId in Heartbeat: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in Heartbeat: required")
	}
	if _, ok := raw["lastBootPhase"]; raw != nil && !ok {
		return fmt.Errorf("field lastBootPhase in Heartbeat: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in Heartbeat: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in Heartbeat: required")
	}
	if _, ok := raw["timestamp"]; raw != nil && !ok {
		return fmt.Errorf("field timestamp in Heartbeat: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in Heartbeat: required")
	}
	type Plain Heartbeat
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "heartbeat" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "heartbeat")
	}
	*j = Heartbeat(plain)
	return nil
}

// Dispatch a turn. Carries author identity (scmName/scmEmail) for git commit
// attribution (§6.1) and the plan-mode toggle (§8.1).
type Prompt struct {
	// OpenCode conversation id to resume. Absent or null both mean 'start a fresh
	// conversation' (§3.3) — this is the one field in /contracts where omission and
	// explicit null are deliberately synonymous.
	ConversationId PromptConversationId `json:"conversationId,omitempty,omitzero" yaml:"conversationId,omitempty" mapstructure:"conversationId,omitempty"`

	// Reasoning-effort override for this turn; null means use the default.
	Effort PromptEffort `json:"effort" yaml:"effort" mapstructure:"effort"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// Model override for this turn; null means use the session/plan default.
	Model PromptModel `json:"model" yaml:"model" mapstructure:"model"`

	// §8.1: dispatch into plan mode instead of direct execution.
	PlanMode bool `json:"planMode,omitempty,omitzero" yaml:"planMode,omitempty" mapstructure:"planMode,omitempty"`

	// Git author email for commit attribution.
	ScmEmail string `json:"scmEmail" yaml:"scmEmail" mapstructure:"scmEmail"`

	// Git author name for commit attribution.
	ScmName string `json:"scmName" yaml:"scmName" mapstructure:"scmName"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// The prompt text.
	Text string `json:"text" yaml:"text" mapstructure:"text"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// OpenCode conversation id to resume. Absent or null both mean 'start a fresh
// conversation' (§3.3) — this is the one field in /contracts where omission and
// explicit null are deliberately synonymous.
type PromptConversationId *string

// Reasoning-effort override for this turn; null means use the default.
type PromptEffort *string

// Model override for this turn; null means use the session/plan default.
type PromptModel *string

// UnmarshalJSON implements json.Unmarshaler.
func (j *Prompt) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["effort"]; raw != nil && !ok {
		return fmt.Errorf("field effort in Prompt: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in Prompt: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in Prompt: required")
	}
	if _, ok := raw["model"]; raw != nil && !ok {
		return fmt.Errorf("field model in Prompt: required")
	}
	if _, ok := raw["scmEmail"]; raw != nil && !ok {
		return fmt.Errorf("field scmEmail in Prompt: required")
	}
	if _, ok := raw["scmName"]; raw != nil && !ok {
		return fmt.Errorf("field scmName in Prompt: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in Prompt: required")
	}
	if _, ok := raw["text"]; raw != nil && !ok {
		return fmt.Errorf("field text in Prompt: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in Prompt: required")
	}
	type Plain Prompt
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if v, ok := raw["planMode"]; !ok || v == nil {
		plain.PlanMode = false
	}
	if plain.Type != "prompt" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "prompt")
	}
	*j = Prompt(plain)
	return nil
}

// Push one or more repos. §6.1: CP awaits push_complete within 360s (that timeout
// lives in a later PR's platform/timeouts.go, not here).
type Push struct {
	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// Per-repo push spec (§6.1). Repos are always a list (§3.4) — no scalar
	// single-repo mirror.
	Repos []PushReposElem `json:"repos" yaml:"repos" mapstructure:"repos"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// CRITICAL (requires ackId).
type PushComplete struct {
	// Deterministic ackId = 'push_complete:{messageId}' (§6.1).
	AckId string `json:"ackId" yaml:"ackId" mapstructure:"ackId"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// Repos corresponds to the JSON schema field "repos".
	Repos []PushCompleteReposElem `json:"repos" yaml:"repos" mapstructure:"repos"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

type PushCompleteReposElem struct {
	// Branch corresponds to the JSON schema field "branch".
	Branch string `json:"branch" yaml:"branch" mapstructure:"branch"`

	// Name corresponds to the JSON schema field "name".
	Name string `json:"name" yaml:"name" mapstructure:"name"`

	// Sha corresponds to the JSON schema field "sha".
	Sha string `json:"sha" yaml:"sha" mapstructure:"sha"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *PushCompleteReposElem) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["branch"]; raw != nil && !ok {
		return fmt.Errorf("field branch in PushCompleteReposElem: required")
	}
	if _, ok := raw["name"]; raw != nil && !ok {
		return fmt.Errorf("field name in PushCompleteReposElem: required")
	}
	if _, ok := raw["sha"]; raw != nil && !ok {
		return fmt.Errorf("field sha in PushCompleteReposElem: required")
	}
	type Plain PushCompleteReposElem
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	*j = PushCompleteReposElem(plain)
	return nil
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *PushComplete) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["ackId"]; raw != nil && !ok {
		return fmt.Errorf("field ackId in PushComplete: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in PushComplete: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in PushComplete: required")
	}
	if _, ok := raw["repos"]; raw != nil && !ok {
		return fmt.Errorf("field repos in PushComplete: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in PushComplete: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in PushComplete: required")
	}
	type Plain PushComplete
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if matched, _ := regexp.MatchString(`^push_complete:.+$`, string(plain.AckId)); !matched {
		return fmt.Errorf("field %s pattern match: must match %s", "AckId", `^push_complete:.+$`)
	}
	if plain.Type != "push_complete" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "push_complete")
	}
	*j = PushComplete(plain)
	return nil
}

// CRITICAL (requires ackId).
type PushError struct {
	// Deterministic ackId = 'push_error:{messageId}' (§6.1).
	AckId string `json:"ackId" yaml:"ackId" mapstructure:"ackId"`

	// Error corresponds to the JSON schema field "error".
	Error string `json:"error" yaml:"error" mapstructure:"error"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *PushError) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["ackId"]; raw != nil && !ok {
		return fmt.Errorf("field ackId in PushError: required")
	}
	if _, ok := raw["error"]; raw != nil && !ok {
		return fmt.Errorf("field error in PushError: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in PushError: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in PushError: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in PushError: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in PushError: required")
	}
	type Plain PushError
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if matched, _ := regexp.MatchString(`^push_error:.+$`, string(plain.AckId)); !matched {
		return fmt.Errorf("field %s pattern match: must match %s", "AckId", `^push_error:.+$`)
	}
	if plain.Type != "push_error" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "push_error")
	}
	*j = PushError(plain)
	return nil
}

type PushReposElem struct {
	// Branch corresponds to the JSON schema field "branch".
	Branch string `json:"branch" yaml:"branch" mapstructure:"branch"`

	// Name corresponds to the JSON schema field "name".
	Name string `json:"name" yaml:"name" mapstructure:"name"`

	// Remote name override; null means use the repo's configured default remote.
	Remote PushReposElemRemote `json:"remote" yaml:"remote" mapstructure:"remote"`
}

// Remote name override; null means use the repo's configured default remote.
type PushReposElemRemote *string

// UnmarshalJSON implements json.Unmarshaler.
func (j *PushReposElem) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["branch"]; raw != nil && !ok {
		return fmt.Errorf("field branch in PushReposElem: required")
	}
	if _, ok := raw["name"]; raw != nil && !ok {
		return fmt.Errorf("field name in PushReposElem: required")
	}
	if _, ok := raw["remote"]; raw != nil && !ok {
		return fmt.Errorf("field remote in PushReposElem: required")
	}
	type Plain PushReposElem
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	*j = PushReposElem(plain)
	return nil
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *Push) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in Push: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in Push: required")
	}
	if _, ok := raw["repos"]; raw != nil && !ok {
		return fmt.Errorf("field repos in Push: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in Push: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in Push: required")
	}
	type Plain Push
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Repos != nil && len(plain.Repos) < 1 {
		return fmt.Errorf("field %s length: must be >= %d", "repos", 1)
	}
	if plain.Type != "push" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "push")
	}
	*j = Push(plain)
	return nil
}

// First event on a fresh WS connection, once the agent is ready to receive
// commands.
type Ready struct {
	// §12.2 item 1's own runtime-fingerprint gap (§5.3's own boot fingerprint,
	// sandboxboot.BootFingerprint.AgentVersion): this gen's own sandbox-agent binary
	// version. Always a real, non-empty string -- 'dev' when NARVI_AGENT_VERSION is
	// unset (no build-time version-stamping pipeline exists yet), never omitted.
	AgentVersion string `json:"agentVersion" yaml:"agentVersion" mapstructure:"agentVersion"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// §12.2 item 1's own runtime-fingerprint gap
	// (sandboxboot.BootFingerprint.ImageDigest): this gen's own sandbox image digest.
	// Always a real, non-empty string -- 'unknown' when NARVI_IMAGE_DIGEST is unset
	// (a documented, honest gap: no mechanism yet injects it into a spawned sandbox),
	// never omitted.
	ImageDigest string `json:"imageDigest" yaml:"imageDigest" mapstructure:"imageDigest"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Timestamp corresponds to the JSON schema field "timestamp".
	Timestamp time.Time `json:"timestamp" yaml:"timestamp" mapstructure:"timestamp"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *Ready) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["agentVersion"]; raw != nil && !ok {
		return fmt.Errorf("field agentVersion in Ready: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in Ready: required")
	}
	if _, ok := raw["imageDigest"]; raw != nil && !ok {
		return fmt.Errorf("field imageDigest in Ready: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in Ready: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in Ready: required")
	}
	if _, ok := raw["timestamp"]; raw != nil && !ok {
		return fmt.Errorf("field timestamp in Ready: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in Ready: required")
	}
	type Plain Ready
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "ready" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "ready")
	}
	*j = Ready(plain)
	return nil
}

// CRITICAL (requires ackId). Named SandboxErrorEvent rather than Error in this
// schema — a $def literally named Error generates a TS interface that shadows the
// built-in Error class for any consumer that imports it unaliased.
type SandboxErrorEvent struct {
	// Deterministic ackId = 'error:{messageId}' (§6.1).
	AckId string `json:"ackId" yaml:"ackId" mapstructure:"ackId"`

	// Fatal corresponds to the JSON schema field "fatal".
	Fatal bool `json:"fatal" yaml:"fatal" mapstructure:"fatal"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// Message corresponds to the JSON schema field "message".
	Message string `json:"message" yaml:"message" mapstructure:"message"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *SandboxErrorEvent) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["ackId"]; raw != nil && !ok {
		return fmt.Errorf("field ackId in SandboxErrorEvent: required")
	}
	if _, ok := raw["fatal"]; raw != nil && !ok {
		return fmt.Errorf("field fatal in SandboxErrorEvent: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in SandboxErrorEvent: required")
	}
	if _, ok := raw["message"]; raw != nil && !ok {
		return fmt.Errorf("field message in SandboxErrorEvent: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in SandboxErrorEvent: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in SandboxErrorEvent: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in SandboxErrorEvent: required")
	}
	type Plain SandboxErrorEvent
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if matched, _ := regexp.MatchString(`^error:.+$`, string(plain.AckId)); !matched {
		return fmt.Errorf("field %s pattern match: must match %s", "AckId", `^error:.+$`)
	}
	if plain.Type != "error" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "error")
	}
	*j = SandboxErrorEvent(plain)
	return nil
}

type SessionTitle struct {
	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Title corresponds to the JSON schema field "title".
	Title string `json:"title" yaml:"title" mapstructure:"title"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *SessionTitle) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in SessionTitle: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in SessionTitle: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in SessionTitle: required")
	}
	if _, ok := raw["title"]; raw != nil && !ok {
		return fmt.Errorf("field title in SessionTitle: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in SessionTitle: required")
	}
	type Plain SessionTitle
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "session_title" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "session_title")
	}
	*j = SessionTitle(plain)
	return nil
}

// Request an orderly sandbox shutdown. No fields beyond the common envelope.
type Shutdown struct {
	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *Shutdown) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in Shutdown: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in Shutdown: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in Shutdown: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in Shutdown: required")
	}
	type Plain Shutdown
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "shutdown" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "shutdown")
	}
	*j = Shutdown(plain)
	return nil
}

// Request a filesystem snapshot. No fields beyond the common envelope.
type Snapshot struct {
	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// CRITICAL (requires ackId).
type SnapshotReady struct {
	// Deterministic ackId = 'snapshot_ready:{messageId}' (§6.1).
	AckId string `json:"ackId" yaml:"ackId" mapstructure:"ackId"`

	// Echoes the messageId of the Snapshot command this event is completing
	// (sandbox-agent sets this to the exact MessageId of the Snapshot command it
	// received). Optional and additive: this event has zero real production consumers
	// before its own first implementation, so adding this field now is not a breaking
	// wire-contract change. Exists so the control plane can correlate a
	// snapshot_ready back to the specific attempt it answers -- gen alone cannot,
	// since neither the snapshot-start nor snapshot-complete transition is gen-fenced
	// (a snapshot cycle happens within the same gen). Absent/omitted on any producer
	// that predates this field.
	CommandMessageId *string `json:"commandMessageId,omitempty,omitzero" yaml:"commandMessageId,omitempty" mapstructure:"commandMessageId,omitempty"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// SnapshotId corresponds to the JSON schema field "snapshotId".
	SnapshotId string `json:"snapshotId" yaml:"snapshotId" mapstructure:"snapshotId"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *SnapshotReady) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["ackId"]; raw != nil && !ok {
		return fmt.Errorf("field ackId in SnapshotReady: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in SnapshotReady: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in SnapshotReady: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in SnapshotReady: required")
	}
	if _, ok := raw["snapshotId"]; raw != nil && !ok {
		return fmt.Errorf("field snapshotId in SnapshotReady: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in SnapshotReady: required")
	}
	type Plain SnapshotReady
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if matched, _ := regexp.MatchString(`^snapshot_ready:.+$`, string(plain.AckId)); !matched {
		return fmt.Errorf("field %s pattern match: must match %s", "AckId", `^snapshot_ready:.+$`)
	}
	if plain.Type != "snapshot_ready" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "snapshot_ready")
	}
	*j = SnapshotReady(plain)
	return nil
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *Snapshot) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in Snapshot: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in Snapshot: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in Snapshot: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in Snapshot: required")
	}
	type Plain Snapshot
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "snapshot" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "snapshot")
	}
	*j = Snapshot(plain)
	return nil
}

// THE CRITICAL SCHEMA (§6.1 / §9.1 dedicated test): cost.tokens is an OBJECT,
// never a bare number. A number-vs-object mismatch here silently zeroes cost
// tracking downstream, so this shape is pinned by a dedicated round-trip +
// rejection test in contracts/contractstest.
type StepFinish struct {
	// Cost corresponds to the JSON schema field "cost".
	Cost StepFinishCost `json:"cost" yaml:"cost" mapstructure:"cost"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// StepId corresponds to the JSON schema field "stepId".
	StepId string `json:"stepId" yaml:"stepId" mapstructure:"stepId"`

	// §6.1/§7.1 sub-task fan-out: OPTIONAL — absent or null means this event belongs
	// to the turn's main lane; a non-null value is the subTaskId (same id
	// sub_task_start/sub_task_finish carry) of the sub-task lane this event belongs
	// to.
	SubTaskId StepFinishSubTaskId `json:"subTaskId,omitempty,omitzero" yaml:"subTaskId,omitempty" mapstructure:"subTaskId,omitempty"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

type StepFinishCost struct {
	// NOTE: tokens is an object, not a number (§6.1 explicit warning).
	Tokens StepFinishCostTokens `json:"tokens" yaml:"tokens" mapstructure:"tokens"`

	// Usd corresponds to the JSON schema field "usd".
	Usd StepFinishCostUsd `json:"usd,omitempty,omitzero" yaml:"usd,omitempty" mapstructure:"usd,omitempty"`
}

// NOTE: tokens is an object, not a number (§6.1 explicit warning).
type StepFinishCostTokens struct {
	// Cached corresponds to the JSON schema field "cached".
	Cached StepFinishCostTokensCached `json:"cached,omitempty,omitzero" yaml:"cached,omitempty" mapstructure:"cached,omitempty"`

	// Input corresponds to the JSON schema field "input".
	Input int `json:"input" yaml:"input" mapstructure:"input"`

	// Output corresponds to the JSON schema field "output".
	Output int `json:"output" yaml:"output" mapstructure:"output"`
}

type StepFinishCostTokensCached *int

// UnmarshalJSON implements json.Unmarshaler.
func (j *StepFinishCostTokens) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["input"]; raw != nil && !ok {
		return fmt.Errorf("field input in StepFinishCostTokens: required")
	}
	if _, ok := raw["output"]; raw != nil && !ok {
		return fmt.Errorf("field output in StepFinishCostTokens: required")
	}
	type Plain StepFinishCostTokens
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	*j = StepFinishCostTokens(plain)
	return nil
}

type StepFinishCostUsd *float64

// UnmarshalJSON implements json.Unmarshaler.
func (j *StepFinishCost) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["tokens"]; raw != nil && !ok {
		return fmt.Errorf("field tokens in StepFinishCost: required")
	}
	type Plain StepFinishCost
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	*j = StepFinishCost(plain)
	return nil
}

// §6.1/§7.1 sub-task fan-out: OPTIONAL — absent or null means this event belongs
// to the turn's main lane; a non-null value is the subTaskId (same id
// sub_task_start/sub_task_finish carry) of the sub-task lane this event belongs
// to.
type StepFinishSubTaskId *string

// UnmarshalJSON implements json.Unmarshaler.
func (j *StepFinish) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["cost"]; raw != nil && !ok {
		return fmt.Errorf("field cost in StepFinish: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in StepFinish: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in StepFinish: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in StepFinish: required")
	}
	if _, ok := raw["stepId"]; raw != nil && !ok {
		return fmt.Errorf("field stepId in StepFinish: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in StepFinish: required")
	}
	type Plain StepFinish
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "step_finish" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "step_finish")
	}
	*j = StepFinish(plain)
	return nil
}

type StepStart struct {
	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// StepId corresponds to the JSON schema field "stepId".
	StepId string `json:"stepId" yaml:"stepId" mapstructure:"stepId"`

	// §6.1/§7.1 sub-task fan-out: OPTIONAL — absent or null means this event belongs
	// to the turn's main lane; a non-null value is the subTaskId (same id
	// sub_task_start/sub_task_finish carry) of the sub-task lane this event belongs
	// to.
	SubTaskId StepStartSubTaskId `json:"subTaskId,omitempty,omitzero" yaml:"subTaskId,omitempty" mapstructure:"subTaskId,omitempty"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// §6.1/§7.1 sub-task fan-out: OPTIONAL — absent or null means this event belongs
// to the turn's main lane; a non-null value is the subTaskId (same id
// sub_task_start/sub_task_finish carry) of the sub-task lane this event belongs
// to.
type StepStartSubTaskId *string

// UnmarshalJSON implements json.Unmarshaler.
func (j *StepStart) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in StepStart: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in StepStart: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in StepStart: required")
	}
	if _, ok := raw["stepId"]; raw != nil && !ok {
		return fmt.Errorf("field stepId in StepStart: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in StepStart: required")
	}
	type Plain StepStart
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "step_start" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "step_start")
	}
	*j = StepStart(plain)
	return nil
}

// Cancel the in-flight turn. No fields beyond the common envelope.
type Stop struct {
	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *Stop) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in Stop: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in Stop: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in Stop: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in Stop: required")
	}
	type Plain Stop
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "stop" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "stop")
	}
	*j = Stop(plain)
	return nil
}

// CRITICAL (requires ackId). §7.1: closes an 'active' state the UI tracks (a live
// sub-lane count), the same criticality reasoning as execution_complete at the
// turn level.
type SubTaskFinish struct {
	// Deterministic ackId = 'sub_task_finish:{messageId}' (§6.1).
	AckId string `json:"ackId" yaml:"ackId" mapstructure:"ackId"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// Reuses the turn's own outcome taxonomy (§3.3, §7.1).
	Outcome SubTaskFinishOutcome `json:"outcome" yaml:"outcome" mapstructure:"outcome"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Same subTaskId this sub-task's own sub_task_start carried.
	SubTaskId string `json:"subTaskId" yaml:"subTaskId" mapstructure:"subTaskId"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

type SubTaskFinishOutcome string

const SubTaskFinishOutcomeCancelled SubTaskFinishOutcome = "cancelled"
const SubTaskFinishOutcomeCompleted SubTaskFinishOutcome = "completed"
const SubTaskFinishOutcomeFailed SubTaskFinishOutcome = "failed"

var enumValues_SubTaskFinishOutcome = []interface{}{
	"completed",
	"failed",
	"cancelled",
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *SubTaskFinishOutcome) UnmarshalJSON(value []byte) error {
	var v string
	if err := json.Unmarshal(value, &v); err != nil {
		return err
	}
	var ok bool
	for _, expected := range enumValues_SubTaskFinishOutcome {
		if reflect.DeepEqual(v, expected) {
			ok = true
			break
		}
	}
	if !ok {
		return fmt.Errorf("invalid value (expected one of %#v): %#v", enumValues_SubTaskFinishOutcome, v)
	}
	*j = SubTaskFinishOutcome(v)
	return nil
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *SubTaskFinish) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["ackId"]; raw != nil && !ok {
		return fmt.Errorf("field ackId in SubTaskFinish: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in SubTaskFinish: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in SubTaskFinish: required")
	}
	if _, ok := raw["outcome"]; raw != nil && !ok {
		return fmt.Errorf("field outcome in SubTaskFinish: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in SubTaskFinish: required")
	}
	if _, ok := raw["subTaskId"]; raw != nil && !ok {
		return fmt.Errorf("field subTaskId in SubTaskFinish: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in SubTaskFinish: required")
	}
	type Plain SubTaskFinish
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if matched, _ := regexp.MatchString(`^sub_task_finish:.+$`, string(plain.AckId)); !matched {
		return fmt.Errorf("field %s pattern match: must match %s", "AckId", `^sub_task_finish:.+$`)
	}
	if plain.Type != "sub_task_finish" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "sub_task_finish")
	}
	*j = SubTaskFinish(plain)
	return nil
}

// §7.1: brackets a spawned sub-task's lifetime. NOT critical (no ackId) — only
// sub_task_finish closes an 'active' state the ack protocol must guarantee
// delivery of.
type SubTaskStart struct {
	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// Human-readable sub-task label (e.g. OpenCode's own subtask part 'description'
	// field).
	Label string `json:"label" yaml:"label" mapstructure:"label"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// The messageId of the enclosing main-lane message whose invocation spawned this
	// sub-task.
	ParentMessageId string `json:"parentMessageId" yaml:"parentMessageId" mapstructure:"parentMessageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// §26.4 (§26.4/§7.1): the task tool's own 'subagent_type' dispatch parameter --
	// the literal named sub-agent (e.g. 'counter-reviewer', 'architecture-scribe',
	// 'fact-check') the engine was actually told to invoke, VERIFIED LIVE as one of
	// the task tool's own real input fields
	// ({"description","prompt","subagent_type"}). Unlike label above (freeform,
	// explicitly documented there as 'not a correctness-bearing value'), this is the
	// engine's own reliable dispatch parameter, which is why post-hoc sub-task
	// corroboration (reviewverdict.CounterReviewCorroborated) keys off this field,
	// never off label. Optional and additive: this event has real, already-shipped
	// production consumers that predate this field, so adding it now is not a
	// breaking wire-contract change. Absent/omitted on any producer that predates
	// this field, and always absent on the legacy/unverified-live subtaskPart
	// fallback translation path (translateSubTaskStart), which has no task-tool input
	// to extract this from at all.
	SubAgentType *string `json:"subAgentType,omitempty,omitzero" yaml:"subAgentType,omitempty" mapstructure:"subAgentType,omitempty"`

	// Stable correlator for this sub-task's lifetime (§7.1), derived from whatever
	// correlator the engine itself exposes (OpenCode's own nested-task id today).
	SubTaskId string `json:"subTaskId" yaml:"subTaskId" mapstructure:"subTaskId"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *SubTaskStart) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in SubTaskStart: required")
	}
	if _, ok := raw["label"]; raw != nil && !ok {
		return fmt.Errorf("field label in SubTaskStart: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in SubTaskStart: required")
	}
	if _, ok := raw["parentMessageId"]; raw != nil && !ok {
		return fmt.Errorf("field parentMessageId in SubTaskStart: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in SubTaskStart: required")
	}
	if _, ok := raw["subTaskId"]; raw != nil && !ok {
		return fmt.Errorf("field subTaskId in SubTaskStart: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in SubTaskStart: required")
	}
	type Plain SubTaskStart
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "sub_task_start" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "sub_task_start")
	}
	*j = SubTaskStart(plain)
	return nil
}

// §6.1: text is CUMULATIVE, not a delta. Consumers MUST treat this as a full
// replacement of the streamed text keyed by messageId (upsert-by-messageId), never
// append the text onto a running buffer.
type Token struct {
	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// §6.1/§7.1 sub-task fan-out: OPTIONAL — absent or null means this event belongs
	// to the turn's main lane; a non-null value is the subTaskId (same id
	// sub_task_start/sub_task_finish carry) of the sub-task lane this event belongs
	// to.
	SubTaskId TokenSubTaskId `json:"subTaskId,omitempty,omitzero" yaml:"subTaskId,omitempty" mapstructure:"subTaskId,omitempty"`

	// The full cumulative text so far for this messageId — replace, do not append.
	Text string `json:"text" yaml:"text" mapstructure:"text"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// §6.1/§7.1 sub-task fan-out: OPTIONAL — absent or null means this event belongs
// to the turn's main lane; a non-null value is the subTaskId (same id
// sub_task_start/sub_task_finish carry) of the sub-task lane this event belongs
// to.
type TokenSubTaskId *string

// UnmarshalJSON implements json.Unmarshaler.
func (j *Token) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in Token: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in Token: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in Token: required")
	}
	if _, ok := raw["text"]; raw != nil && !ok {
		return fmt.Errorf("field text in Token: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in Token: required")
	}
	type Plain Token
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "token" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "token")
	}
	*j = Token(plain)
	return nil
}

type ToolCall struct {
	// CallId corresponds to the JSON schema field "callId".
	CallId string `json:"callId" yaml:"callId" mapstructure:"callId"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// Freeform, tool-specific input.
	Input ToolCallInput `json:"input" yaml:"input" mapstructure:"input"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// §6.1/§7.1 sub-task fan-out: OPTIONAL — absent or null means this event belongs
	// to the turn's main lane; a non-null value is the subTaskId (same id
	// sub_task_start/sub_task_finish carry) of the sub-task lane this event belongs
	// to.
	SubTaskId ToolCallSubTaskId `json:"subTaskId,omitempty,omitzero" yaml:"subTaskId,omitempty" mapstructure:"subTaskId,omitempty"`

	// ToolName corresponds to the JSON schema field "toolName".
	ToolName string `json:"toolName" yaml:"toolName" mapstructure:"toolName"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// Freeform, tool-specific input.
type ToolCallInput map[string]interface{}

// §6.1/§7.1 sub-task fan-out: OPTIONAL — absent or null means this event belongs
// to the turn's main lane; a non-null value is the subTaskId (same id
// sub_task_start/sub_task_finish carry) of the sub-task lane this event belongs
// to.
type ToolCallSubTaskId *string

// UnmarshalJSON implements json.Unmarshaler.
func (j *ToolCall) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["callId"]; raw != nil && !ok {
		return fmt.Errorf("field callId in ToolCall: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in ToolCall: required")
	}
	if _, ok := raw["input"]; raw != nil && !ok {
		return fmt.Errorf("field input in ToolCall: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in ToolCall: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in ToolCall: required")
	}
	if _, ok := raw["toolName"]; raw != nil && !ok {
		return fmt.Errorf("field toolName in ToolCall: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in ToolCall: required")
	}
	type Plain ToolCall
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "tool_call" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "tool_call")
	}
	*j = ToolCall(plain)
	return nil
}

type ToolResult struct {
	// CallId corresponds to the JSON schema field "callId".
	CallId string `json:"callId" yaml:"callId" mapstructure:"callId"`

	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// IsError corresponds to the JSON schema field "isError".
	IsError bool `json:"isError" yaml:"isError" mapstructure:"isError"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// Freeform, tool-specific output.
	Output ToolResultOutput `json:"output" yaml:"output" mapstructure:"output"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// §6.1/§7.1 sub-task fan-out: OPTIONAL — absent or null means this event belongs
	// to the turn's main lane; a non-null value is the subTaskId (same id
	// sub_task_start/sub_task_finish carry) of the sub-task lane this event belongs
	// to.
	SubTaskId ToolResultSubTaskId `json:"subTaskId,omitempty,omitzero" yaml:"subTaskId,omitempty" mapstructure:"subTaskId,omitempty"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// Freeform, tool-specific output.
type ToolResultOutput map[string]interface{}

// §6.1/§7.1 sub-task fan-out: OPTIONAL — absent or null means this event belongs
// to the turn's main lane; a non-null value is the subTaskId (same id
// sub_task_start/sub_task_finish carry) of the sub-task lane this event belongs
// to.
type ToolResultSubTaskId *string

// UnmarshalJSON implements json.Unmarshaler.
func (j *ToolResult) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["callId"]; raw != nil && !ok {
		return fmt.Errorf("field callId in ToolResult: required")
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in ToolResult: required")
	}
	if _, ok := raw["isError"]; raw != nil && !ok {
		return fmt.Errorf("field isError in ToolResult: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in ToolResult: required")
	}
	if _, ok := raw["output"]; raw != nil && !ok {
		return fmt.Errorf("field output in ToolResult: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in ToolResult: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in ToolResult: required")
	}
	type Plain ToolResult
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "tool_result" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "tool_result")
	}
	*j = ToolResult(plain)
	return nil
}

type Warning struct {
	// Gen corresponds to the JSON schema field "gen".
	Gen int `json:"gen" yaml:"gen" mapstructure:"gen"`

	// Message corresponds to the JSON schema field "message".
	Message string `json:"message" yaml:"message" mapstructure:"message"`

	// MessageId corresponds to the JSON schema field "messageId".
	MessageId string `json:"messageId" yaml:"messageId" mapstructure:"messageId"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Type corresponds to the JSON schema field "type".
	Type string `json:"type" yaml:"type" mapstructure:"type"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *Warning) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["gen"]; raw != nil && !ok {
		return fmt.Errorf("field gen in Warning: required")
	}
	if _, ok := raw["message"]; raw != nil && !ok {
		return fmt.Errorf("field message in Warning: required")
	}
	if _, ok := raw["messageId"]; raw != nil && !ok {
		return fmt.Errorf("field messageId in Warning: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in Warning: required")
	}
	if _, ok := raw["type"]; raw != nil && !ok {
		return fmt.Errorf("field type in Warning: required")
	}
	type Plain Warning
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.Type != "warning" {
		return fmt.Errorf("field %s: must be equal to %s", "type", "warning")
	}
	*j = Warning(plain)
	return nil
}
