// Code generated by github.com/atombender/go-jsonschema, DO NOT EDIT.

package clientws

import "encoding/json"
import "fmt"

type FetchHistoryRequest struct {
	// Opaque pagination cursor; null to start from the beginning/most recent
	// (server-defined).
	Cursor FetchHistoryRequestCursor `json:"cursor" yaml:"cursor" mapstructure:"cursor"`

	// Null means use the server default page size.
	Limit FetchHistoryRequestLimit `json:"limit" yaml:"limit" mapstructure:"limit"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`
}

// Opaque pagination cursor; null to start from the beginning/most recent
// (server-defined).
type FetchHistoryRequestCursor *string

// Null means use the server default page size.
type FetchHistoryRequestLimit *int

// UnmarshalJSON implements json.Unmarshaler.
func (j *FetchHistoryRequest) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["cursor"]; raw != nil && !ok {
		return fmt.Errorf("field cursor in FetchHistoryRequest: required")
	}
	if _, ok := raw["limit"]; raw != nil && !ok {
		return fmt.Errorf("field limit in FetchHistoryRequest: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in FetchHistoryRequest: required")
	}
	type Plain FetchHistoryRequest
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	*j = FetchHistoryRequest(plain)
	return nil
}

type FetchHistoryResponse struct {
	// Events corresponds to the JSON schema field "events".
	Events []FetchHistoryResponseEventsElem `json:"events" yaml:"events" mapstructure:"events"`

	// Null when there are no more pages.
	NextCursor FetchHistoryResponseNextCursor `json:"nextCursor" yaml:"nextCursor" mapstructure:"nextCursor"`
}

type FetchHistoryResponseEventsElem map[string]interface{}

// Null when there are no more pages.
type FetchHistoryResponseNextCursor *string

// UnmarshalJSON implements json.Unmarshaler.
func (j *FetchHistoryResponse) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["events"]; raw != nil && !ok {
		return fmt.Errorf("field events in FetchHistoryResponse: required")
	}
	if _, ok := raw["nextCursor"]; raw != nil && !ok {
		return fmt.Errorf("field nextCursor in FetchHistoryResponse: required")
	}
	type Plain FetchHistoryResponse
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	*j = FetchHistoryResponse(plain)
	return nil
}

// Sent by the browser client immediately after connecting; must arrive within 30s
// (§6.2).
type SubscribeRequest struct {
	// ClientId corresponds to the JSON schema field "clientId".
	ClientId string `json:"clientId" yaml:"clientId" mapstructure:"clientId"`

	// Per-participant WS token minted via REST (/api/sessions/:id/ws-token).
	Token string `json:"token" yaml:"token" mapstructure:"token"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *SubscribeRequest) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["clientId"]; raw != nil && !ok {
		return fmt.Errorf("field clientId in SubscribeRequest: required")
	}
	if _, ok := raw["token"]; raw != nil && !ok {
		return fmt.Errorf("field token in SubscribeRequest: required")
	}
	type Plain SubscribeRequest
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	*j = SubscribeRequest(plain)
	return nil
}

// The single reply to subscribe. state/events/artifacts/participants are
// deliberately loosely typed here (additionalProperties: true) — the full
// session/turn/sandbox read-model shape is assembled by later PRs; this schema
// only fixes the top-level envelope.
type SubscribedPayload struct {
	// Artifacts corresponds to the JSON schema field "artifacts".
	Artifacts []SubscribedPayloadArtifactsElem `json:"artifacts" yaml:"artifacts" mapstructure:"artifacts"`

	// Event replay.
	Events []SubscribedPayloadEventsElem `json:"events" yaml:"events" mapstructure:"events"`

	// True when events is a cut prefix of this session's full history rather than all
	// of it -- the fixed item-count cap or the byte-size budget was hit. False means
	// events is the session's complete history to date. A client that needs what was
	// cut already has fetch_history.
	EventsTruncated bool `json:"eventsTruncated" yaml:"eventsTruncated" mapstructure:"eventsTruncated"`

	// Participants corresponds to the JSON schema field "participants".
	Participants []SubscribedPayloadParticipantsElem `json:"participants" yaml:"participants" mapstructure:"participants"`

	// SessionId corresponds to the JSON schema field "sessionId".
	SessionId string `json:"sessionId" yaml:"sessionId" mapstructure:"sessionId"`

	// Full session/turn/sandbox read model (shape assembled by later PRs).
	State SubscribedPayloadState `json:"state" yaml:"state" mapstructure:"state"`
}

type SubscribedPayloadArtifactsElem map[string]interface{}

type SubscribedPayloadEventsElem map[string]interface{}

type SubscribedPayloadParticipantsElem map[string]interface{}

// Full session/turn/sandbox read model (shape assembled by later PRs).
type SubscribedPayloadState map[string]interface{}

// UnmarshalJSON implements json.Unmarshaler.
func (j *SubscribedPayload) UnmarshalJSON(value []byte) error {
	var raw map[string]interface{}
	if err := json.Unmarshal(value, &raw); err != nil {
		return err
	}
	if _, ok := raw["artifacts"]; raw != nil && !ok {
		return fmt.Errorf("field artifacts in SubscribedPayload: required")
	}
	if _, ok := raw["events"]; raw != nil && !ok {
		return fmt.Errorf("field events in SubscribedPayload: required")
	}
	if _, ok := raw["eventsTruncated"]; raw != nil && !ok {
		return fmt.Errorf("field eventsTruncated in SubscribedPayload: required")
	}
	if _, ok := raw["participants"]; raw != nil && !ok {
		return fmt.Errorf("field participants in SubscribedPayload: required")
	}
	if _, ok := raw["sessionId"]; raw != nil && !ok {
		return fmt.Errorf("field sessionId in SubscribedPayload: required")
	}
	if _, ok := raw["state"]; raw != nil && !ok {
		return fmt.Errorf("field state in SubscribedPayload: required")
	}
	type Plain SubscribedPayload
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	*j = SubscribedPayload(plain)
	return nil
}
